/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['dhhp.edu.vn'],
        unoptimized: true
    }
};

export default nextConfig;
